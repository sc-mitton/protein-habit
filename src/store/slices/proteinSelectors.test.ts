import {
  selectAggregates,
  selectDailyProteinTarget,
  selectTotalProteinForDay,
  selectWeeklyAvg,
  selectStreak,
  selectDailyTargetResults,
  selectTodaysEntries,
} from "./proteinSelectors";

function sum(a: number, b: number) {
  return a + b;
}

test("adds 1 + 2 to equal 3", () => {});
