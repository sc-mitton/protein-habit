import { Box } from "@components";
import { RootScreenProps } from "@types";

const RecipesScreen = (props: RootScreenProps<"Recipes">) => {
  return <Box flex={1} backgroundColor="mainBackground"></Box>;
};

export default RecipesScreen;
